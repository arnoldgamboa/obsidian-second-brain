1. Create a sudo user
		adduser username
		- usermod -aG sudo username

1. Install NPM

		sudo apt update
		- sudo apt install npm


1. Install yarn

Sudo nom install -g yarn

1. Copy authorized id

cp ~/.ssh/authorized_keys /home/arnold

1. Create ssh keys

ssh-keygen

1. Install nvm 

https://github.com/nvm-sh/nvm